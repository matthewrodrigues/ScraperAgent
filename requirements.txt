anthropic==0.107.1
fastapi==0.136.3
uvicorn==0.49.0
httpx==0.28.1
pydantic==2.13.4
python-dotenv==1.2.2
python-multipart==0.0.20
jinja2==3.1.4
itsdangerous==2.2.0  # signs the dashboard session cookie via Starlette's SessionMiddleware
requests==2.34.2
PyYAML==6.0.3
defusedxml==0.7.1

# LangGraph + checkpointing
langchain==1.3.4
langchain-anthropic==1.4.4
langchain-core==1.4.2
langgraph==1.2.4
langgraph-checkpoint==4.1.1
langgraph-checkpoint-sqlite==2.0.10
langgraph-prebuilt==1.1.0
langgraph-sdk==0.4.2

# Integrations (added as build steps reach them)
apify-client==3.0.2
google-api-python-client==2.151.0
google-auth-oauthlib==1.2.1
google-auth-httplib2==0.2.0

# Postgres driver for the key broker's Supabase backend. Binary wheel so
# Windows needs no build toolchain.
psycopg[binary]==3.2.3
psycopg-pool==3.2.4

# Browser automation — used for buyer-initiated Best Offer placement, since
# eBay's Trading API PlaceOffer is unreachable for OAuth tokens on this account
# type (see memory `feedback_ebay_trading_gotchas.md` §3 + `project_ebay_browser_automation.md`).
# Headed Chromium with a persistent storage_state.json; first-time login via
# `scratch_ebay_browser_login.py`.
playwright==1.60.0

# Windows SSL passthrough (Norton intercepts HTTPS). Marked win32-only so CI
# and any future Linux container skip a workaround for a problem they don't
# have — config.py guards the matching imports the same way.
pip-system-certs==4.0; sys_platform == "win32"
truststore==0.10.4; sys_platform == "win32"
pytest==8.3.4
