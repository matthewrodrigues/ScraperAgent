.venv/
.env
browser_profile/
screenshots/
__pycache__/
*.pyc
.pytest_cache/

# Secrets dir — keep the directory structure (.gitkeep) but exclude every
# actual secret file (session state, OAuth tokens, etc.)
secrets/*
!secrets/.gitkeep
!secrets/screenshots/
secrets/screenshots/*
!secrets/screenshots/.gitkeep

# Playwright
node_modules/
/test-results/
/playwright-report/
/blob-report/
/playwright/.cache/
/playwright/.auth/
